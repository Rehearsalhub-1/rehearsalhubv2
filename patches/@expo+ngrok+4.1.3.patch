diff --git a/node_modules/@expo/ngrok/index.js b/node_modules/@expo/ngrok/index.js
index c9e5f8d..54d25f4 100644
--- a/node_modules/@expo/ngrok/index.js
+++ b/node_modules/@expo/ngrok/index.js
@@ -25,11 +25,27 @@ async function connect(opts) {
 }
 
 async function connectRetry(opts, retryCount = 0) {
-  opts.name = String(opts.name || uuid.v4());
+  const tunnelOpts = Object.assign({}, opts);
+  tunnelOpts.name = 't' + Date.now() + Math.random().toString(36).slice(2, 6);
+  tunnelOpts.addr = String(tunnelOpts.addr || tunnelOpts.port || 8081);
+  delete tunnelOpts.port;
+  delete tunnelOpts.host;
+  delete tunnelOpts.configPath;
   try {
-    const response = await ngrokClient.startTunnel(opts);
+    const response = await ngrokClient.startTunnel(tunnelOpts);
     return response.public_url;
   } catch (err) {
+    const detailErr = (err?.body?.details?.err || err?.message || '').toLowerCase();
+    if (detailErr.includes('already exists') || err?.body?.error_code === 102) {
+      try {
+        const tunnelsRes = await ngrokClient.listTunnels();
+        const tunnels = tunnelsRes?.tunnels || [];
+        const existing = tunnels.find(t => t.config?.addr === tunnelOpts.addr || (t.proto === tunnelOpts.proto && t.public_url)) || tunnels[0];
+        if (existing && existing.public_url) {
+          return existing.public_url;
+        }
+      } catch (e) {}
+    }
     if (!isRetriable(err) || retryCount >= 100) {
       throw err;
     }
diff --git a/node_modules/@expo/ngrok/src/client.js b/node_modules/@expo/ngrok/src/client.js
index 3f748a3..318c5bc 100644
--- a/node_modules/@expo/ngrok/src/client.js
+++ b/node_modules/@expo/ngrok/src/client.js
@@ -29,17 +29,26 @@ class NgrokClient {
     } catch (error) {
       let clientError;
       try {
-        const response = JSON.parse(error.response.body);
-        clientError = new NgrokClientError(
-          response.msg,
-          error.response,
-          response
-        );
+        const rawBody = error?.response?.body;
+        const response = rawBody ? JSON.parse(rawBody) : null;
+        if (response) {
+          clientError = new NgrokClientError(
+            response.msg || error.message,
+            error.response,
+            response
+          );
+        } else {
+          clientError = new NgrokClientError(
+            error.message,
+            error.response,
+            { msg: error.message }
+          );
+        }
       } catch (e) {
         clientError = new NgrokClientError(
-          error.response.body,
+          error.message,
           error.response,
-          error.response.body
+          { msg: error.message }
         );
       }
       throw clientError;
@@ -52,8 +61,9 @@ class NgrokClient {
         (response) => response.statusCode === 204
       );
     } catch (error) {
-      const response = JSON.parse(error.response.body);
-      throw new NgrokClientError(response.msg, error.response, response);
+      const rawBody = error?.response?.body;
+      const response = rawBody ? (()=>{ try { return JSON.parse(rawBody); } catch { return null; } })() : null;
+      throw new NgrokClientError(response?.msg || error.message, error.response, response || { msg: error.message });
     }
   }
 
diff --git a/node_modules/@expo/ngrok/src/utils.js b/node_modules/@expo/ngrok/src/utils.js
index 1c2288a..2b32b6e 100644
--- a/node_modules/@expo/ngrok/src/utils.js
+++ b/node_modules/@expo/ngrok/src/utils.js
@@ -8,19 +8,25 @@ function defaultConfigPath() {
 }
 
 function defaults(opts) {
-  opts = opts || { proto: "http", addr: 80 };
+  opts = opts || { proto: "http", addr: "80" };
   if (opts.name) {
     const configPath = opts.configPath || defaultConfigPath();
-    const config = parse(readFileSync(configPath, "utf8"));
-    if (config.tunnels && config.tunnels[opts.name]) {
-      opts = Object.assign(opts, config.tunnels[opts.name]);
-    }
+    try {
+      const config = parse(readFileSync(configPath, "utf8"));
+      if (config && config.tunnels && config.tunnels[opts.name]) {
+        opts = Object.assign(opts, config.tunnels[opts.name]);
+      }
+    } catch {}
   }
-  if (typeof opts === "function") opts = { proto: "http", addr: 80 };
+  if (typeof opts === "function") opts = { proto: "http", addr: "80" };
   if (typeof opts !== "object") opts = { proto: "http", addr: opts };
   if (!opts.proto) opts.proto = "http";
-  if (!opts.addr) opts.addr = opts.port || opts.host || 80;
+  if (!opts.addr) opts.addr = String(opts.port || opts.host || 80);
+  else opts.addr = String(opts.addr);
   if (opts.httpauth) opts.auth = opts.httpauth;
+  delete opts.port;
+  delete opts.host;
+  delete opts.configPath;
   return opts;
 }
 
@@ -33,6 +39,10 @@ function validate(opts) {
 }
 
 function isRetriable(err) {
+  const msg = (err?.message || err?.body?.msg || '').toLowerCase();
+  if (msg.includes('econnrefused') || msg.includes('econnreset') || msg.includes('etimedout') || msg.includes('socket hang up')) {
+    return true;
+  }
   if (!err.response) {
     return false;
   }
@@ -41,10 +51,12 @@ function isRetriable(err) {
   const notReady500 = statusCode === 500 && /panic/.test(body);
   const notReady502 =
     statusCode === 502 &&
+    body &&
     body.details &&
     body.details.err === "tunnel session not ready yet";
   const notReady503 =
     statusCode === 503 &&
+    body &&
     body.details &&
     body.details.err ===
       "a successful ngrok tunnel session has not yet been established";
